# TikTok OAuth Configuration
# Get these from TikTok for Developers: https://developers.tiktok.com/
# NOTE: Do not commit real values. Use placeholders only here.
TIKTOK_CLIENT_KEY=your_server_client_key
TIKTOK_CLIENT_SECRET=your_server_client_secret
TIKTOK_REDIRECT_URI=https://your-domain.example.com/api/auth/callback

# App Configuration (Public)
NEXT_PUBLIC_APP_BASE_URL=https://your-domain.example.com

# TikTok Client Key (Public - needed for client-side OAuth)
# This is the public client key (safe to expose to the client)
NEXT_PUBLIC_TIKTOK_CLIENT_KEY=your_public_client_key
NEXT_PUBLIC_TIKTOK_REDIRECT_URI=https://your-domain.example.com/api/auth/callback

# TikTok API Scopes (Public)
# Required scopes: user.info.basic, video.list, user.info.profile
NEXT_PUBLIC_TIKTOK_SCOPES=user.info.basic,video.list,user.info.profile

## Feature flags
# If set to 1, enables the collapsible per-video breakdown on the main page
# Exposed to the client via NEXT_PUBLIC_ENABLE_PER_VIDEO in next.config.js
ENABLE_PER_VIDEO=0

